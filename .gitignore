# Generated locally by `rag ingest`; don't commit them.
store.json
store.index

# Compiled Go binary
/rag
/rag.exe

# Rust build artifacts (the compiled libvindex.a lives here)
rust/vindex/target/

# Editor / OS noise
.DS_Store
